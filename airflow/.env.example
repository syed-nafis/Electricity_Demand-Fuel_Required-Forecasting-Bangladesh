# Copy to .env and fill in. Generate secrets with:
#   AIRFLOW_UID:        id -u
#   AIRFLOW_FERNET_KEY: openssl rand -base64 32
#   AIRFLOW_SECRET_KEY: openssl rand -hex 30
AIRFLOW_UID=50000
AIRFLOW_FERNET_KEY=
AIRFLOW_SECRET_KEY=

# Supabase serving-layer creds (new project after migration)
SUPABASE_URL=
SUPABASE_KEY=
